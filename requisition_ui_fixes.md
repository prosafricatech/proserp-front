# 1. Role selector is misfunctional in adding ne chain level from an exisiting approval chain.

# 2. No monetary fields are shown regardless of the process type at the approval chain setup. (index 3)

# 3. For `LEAVE_REQUEST`, hide or replace the "Create Purchase Order" / "Create Payment" action buttons. Show a "View Leave Records" link that navigates to the HR → Leave Requests list filtered by requisition. (index 8)

    -> no view button that links to the leave  requests. at the leave requests page i dont see requistion data at the response. this is if the requests are linked to a requisition.

# 4. no summary line on an approved leave request view. (index 9)
