export const dynamic = 'force-dynamic';

import UserLedgersMasters from '@/components/accounts/ledgers/userLedgers/UserLedgersMasters';

export default async function Page() {
  return <UserLedgersMasters />;
}
